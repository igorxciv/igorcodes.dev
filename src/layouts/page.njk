{% 	set title =
	global.title if
	page.url == '/' else
	title + (', page - ') + global.title
%}

{% set description =
	description if
	description else
	global.description
%}

{% set cover =
	global.domain + page.url + 'cover.png' if
	cover else
	global.domain + '/images/covers/' + ('index' if
	page.url == '/' else
	page.fileSlug) + '.png'
%}

<!DOCTYPE html>
<html lang="en" class="page">
	<head>
		<meta charset="UTF-8">
		<title>{{ title }}</title>

		<meta name="viewport" content="width=device-width, initial-scale=1">
		<meta name="view-transition" content="same-origin">

		<link rel="stylesheet" href="/styles/index.css">

		<link rel="preload" href="/fonts/inter-variable.woff2" crossorigin as="font" type="font/woff2">

		<link rel="icon" href="/images/logo/favicon.ico" sizes="any">
		<link rel="icon" href="/images/logo/any.svg" type="image/svg+xml">
		<link rel="apple-touch-icon" href="/images/logo/180x180.png">
		<link rel="manifest" href="/manifest.json">

		<link rel="canonical" href="{{ global.domain + page.url }}">

		<meta name="description" content="{{ description | markdownRemove }}">
		<meta name="generator" content="{{ eleventy.generator }}">

		<meta name="theme-color" media="(prefers-color-scheme: light)" content="#f9fafb">
		<meta name="theme-color" media="(prefers-color-scheme: dark)" content="#22262a">

		<meta name="title" content="{{ title }}">
		<meta name="description" content="{{ desc | markdownRemove }}">
		<meta name="content-language" content="en-us">

		<meta property="og:type" content="website">
		<meta property="og:title" content="{{ title }}">
		<meta property="og:description" content="{{ desc | markdownRemove }}">
		<meta property="og:url" content="{{ (global.domain + page.url) }}">

		<meta property="og:image" content="{{ cover }}">
		<meta property="og:image:width" content="600">
		<meta property="og:image:height" content="600">

		<meta name="twitter:card" content="summary_large_image">
		<meta name="twitter:site" content="@igorxciv">

		<style media="(prefers-color-scheme: light)">
			{{ 'src/styles/light.css' | css }}
		</style>
		<style media="(prefers-color-scheme: dark)">
			{{ 'src/styles/dark.css' | css }}
		</style>

		<script src="https://cdn.usefathom.com/script.js" data-site="AQIXEFJP" defer></script>
	</head>
	<body class="page__body">
		{% include 'header.njk' %}

		{{ content | safe }}

		{% include 'footer.njk' %}
	</body>
</html>
